// INSIDE -- it's the page that registered users get access to once logged in. Actually *inside* our social network

import Logout from "./logout";

export default function Inside() {
    return (
        <>
            <h1>🧶 ..aaand we're in! 🧶</h1>
            <Logout />
        </>
    );
}
